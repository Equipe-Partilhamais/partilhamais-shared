import { UF } from '../../types';
import { ItcdTaxType } from './types';
export declare const calculateItcdForState: (uf: UF, baseValue: number, settings: any, deathDate?: string, taxType?: ItcdTaxType) => import("./types").ItcdResult;
